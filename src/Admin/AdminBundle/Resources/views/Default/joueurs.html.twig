{% extends "::baseAdmin.html.twig" %}

{% block body %}

<!-- Call to Action Bar -->
	    <div class="section section-white">
			<div class="container">
				<div class="row">
					<div class="col-md-12">
						<div class="calltoaction-wrapper">
							<h3>Joueurs</h3> 
						</div>
					</div>
				</div>
			</div>
		</div>
		<!-- End Call to Action Bar -->


	<div class="section">
	        <div class="container">
	        	<div class="row">
	        		

		        			<h1>Joueurs inscription en cours</h1>
								
										
							<h3>Joueurs pas valider ni docuement ni paiement</h3>
							{% for joueurDocuementPaiement in joueursDocuementPaiement %}
								<a href="{{ path('admin_joueursInfo', {'id':joueurDocuementPaiement.id}) }}" >{{joueurDocuementPaiement.prenom}} {{joueurDocuementPaiement.nom}}</a><br>
							{%endfor%}

							<br> <br> 

							<h3>Joueurs ou il manque juste la validation de document</h3>
							{% for joueurDocuement in joueursDocuement %}
								<a href="{{ path('admin_joueursInfo', {'id':joueurDocuement.id}) }}" >{{joueurDocuement.prenom}} {{joueurDocuement.nom}}</a><br>
							{%endfor%}

							<br> <br> 

							<h3>Joueurs ou il manque juste la validation du paiement</h3>
							{% for joueurPaiement in joueursPaiement %}
								<a href="{{ path('admin_joueursInfo', {'id':joueurPaiement.id}) }}" >{{joueurPaiement.prenom}} {{joueurPaiement.nom}}</a><br>
							{%endfor%}

							<br> <br> 

							<h1>Joueurs inscrit cette année en 2017</h1>
							{% for joueurLicencie in joueursLicencie %}
								<a href="{{ path('admin_joueursInfo', {'id':joueurLicencie.id}) }}" >{{joueurLicencie.prenom}} {{joueurLicencie.nom}}</a><br>
							{%endfor%}

							
		        	
	        	</div>
	        </div>
	    </div>
	    <!-- End Services -->

		
		











	
{% endblock %}
